import 'package:flutter/material.dart';

class AppColors
{
    static const Color main = Color(0xFFE40050);
    static const Color secundary = Color(0xFF0880DE);

    static const Color background = Color(0xFFECECFE);
    static const Color box = Color(0xFFDEDEFE);
    static const Color card = Color(0xFFC8C8FE);

    static const Color white = Color(0xFFFFFFFF);
    static const Color textDark = Color(0xFF4A4A68);
    static const Color textLight = Color(0xFF828290);
}